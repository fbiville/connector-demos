# Various Neo4j import/export demo

## Kafka CDC sync

Check out `kafka/README.md` to see a live example of a synchronization between a Neo4j source and target, powered by the CDC support in the Neo4j Kafka connector.

## `neo4j-admin` + Neo4j Spark connector

Check `spark/README.md` to learn more about the Neo4j export to GCS.

## Data Importer

This follows the previous example and imports the GCS files into a Neo4j database in Aura.
